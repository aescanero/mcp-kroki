# Kroki MCP Server Configuration

# Kroki server URL (local or remote instance)
KROKI_URL=http://localhost:8000

# Server configuration
HOST=0.0.0.0
PORT=8084

# OAuth 2.1 Authentication Configuration
# Set to "true" to enable OAuth authentication, "false" to disable
OAUTH_ENABLED=false

# OAuth Provider Configuration (only required if OAUTH_ENABLED=true)
# The issuer URL of your OAuth 2.1 provider (e.g., https://auth.example.com)
OAUTH_ISSUER=

# Your OAuth client ID
OAUTH_CLIENT_ID=

# Your OAuth client secret
OAUTH_CLIENT_SECRET=

# Expected audience for the access tokens (optional, but recommended)
OAUTH_AUDIENCE=

# Token validation method: "introspection" or "jwks"
# - introspection: Validates tokens by calling the introspection endpoint (more accurate, requires network call)
# - jwks: Validates tokens locally using JWKS (faster, but may not detect revoked tokens immediately)
OAUTH_TOKEN_VALIDATION=introspection

# OAuth introspection endpoint (required if OAUTH_TOKEN_VALIDATION=introspection)
# e.g., https://auth.example.com/oauth2/introspect
OAUTH_INTROSPECTION_URL=

# JWKS (JSON Web Key Set) URL (required if OAUTH_TOKEN_VALIDATION=jwks)
# e.g., https://auth.example.com/.well-known/jwks.json
OAUTH_JWKS_URL=
